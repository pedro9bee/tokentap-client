FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY pyproject.toml .
COPY tokentap/ tokentap/

RUN pip install --no-cache-dir .

EXPOSE 8080

CMD ["python", "-m", "tokentap.proxy_service"]
